apiVersion: v1
kind: Service
metadata:
  name: {{ include "infraforge-app.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "infraforge-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infraforge-app.selectorLabels" . | nindent 4 }}
